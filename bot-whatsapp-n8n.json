[{"id":"NexusPDVBot001","name":"Nexus PDV — Bot WhatsApp","active":true,"nodes":[{"parameters":{"httpMethod":"POST","path":"nexuspdv-bot","responseMode":"onReceived","options":{}},"id":"n1000000-1111-1111-1111-111111111111","name":"Webhook Evolution","type":"n8n-nodes-base.webhook","typeVersion":2,"position":[180,300],"webhookId":"nexuspdv-bot"},{"parameters":{"jsCode":"// ============================================================\n// PARSE + COOLDOWN — Evolution API (v2), multi-estabelecimento\n// Extrai instância + número do remetente e aplica cooldown de 6h\n// por (instância, número). Ignora grupos, fromMe renova cooldown.\n// ============================================================\nconst body = $input.first().json.body || $input.first().json;\nconst event = body.event || '';\nconst data = body.data || {};\nconst key = data.key || {};\n\n// Instância que originou o evento — usada para roteamento multi-tenant\nconst instance = body.instance || data.instance || '';\nif (!instance) return [];\n\n// Só processa mensagens (ignora QRCODE_UPDATED, CONNECTION_UPDATE, etc.)\nif (event && !/messages\\.upsert|MESSAGES_UPSERT/i.test(event)) return [];\n\n// Ignora grupos\nif ((key.remoteJid || '').includes('@g.us')) return [];\n\n// Extrai o número real (Evolution v2: senderPn/participantPn quando remoteJid=@lid)\nconst candidatos = [key.senderPn, key.participantPn, key.remoteJidAlt, key.participant, data.sender, data.senderPn, data.remoteJidAlt, key.remoteJid].filter(Boolean);\nlet numero = null;\nfor (const cand of candidatos) {\n  if (typeof cand !== 'string') continue;\n  if (cand.includes('@g.us') || cand.endsWith('@lid')) continue;\n  const n = cand.split('@')[0];\n  if (/^\\d{8,15}$/.test(n)) { numero = n; break; }\n}\nif (!numero) return [];\n\n// Cooldown por (instância, número). 6h.\nconst COOLDOWN_MS = 6 * 60 * 60 * 1000;\nconst __sd = $getWorkflowStaticData('global');\nconst cd = __sd.cooldown || {};\nconst chave = `${instance}::${numero}`;\n// Limpa entradas antigas para não crescer indefinidamente\nfor (const [k, ts] of Object.entries(cd)) { if (Date.now() - ts > COOLDOWN_MS) delete cd[k]; }\n\n// Dono respondeu manualmente → renova cooldown e silencia\nif (key.fromMe) {\n  cd[chave] = Date.now();\n  __sd.cooldown = cd;\n  return [];\n}\n\n// Cliente em cooldown → silencia\nif (cd[chave] && Date.now() - cd[chave] < COOLDOWN_MS) return [];\n\n__sd.cooldown = cd;\nreturn [{ json: { instance, numero } }];"},"id":"n2000000-2222-2222-2222-222222222222","name":"Parse + Cooldown","type":"n8n-nodes-base.code","typeVersion":2,"position":[400,300]},{"parameters":{"jsCode":"// ============================================================\n// ROTEAMENTO INSTÂNCIA → URL BASE DO ESTABELECIMENTO\n// Cada estabelecimento tem seu próprio domínio (com sua config,\n// horário, mensagem de alerta etc.). Adicione entradas aqui\n// quando um novo estabelecimento entrar no ar.\n// ============================================================\nconst ROTAS = {\n  // instância na Evolution : URL base do estabelecimento (sem barra final)\n  'nexuspdv': 'https://mercadolojo.com.br',\n  // 'clienteXYZ': 'https://clientexyz.com.br',\n};\n\nconst { instance, numero } = $input.first().json;\nconst baseUrl = ROTAS[instance];\n\nif (!baseUrl) {\n  // Instância desconhecida — silenciar (mas logar) em vez de quebrar\n  console.log('[nexuspdv-bot] instancia sem rota:', instance);\n  return [];\n}\n\nreturn [{ json: { instance, numero, baseUrl } }];"},"id":"n3000000-3333-3333-3333-333333333333","name":"Roteamento Instância→URL","type":"n8n-nodes-base.code","typeVersion":2,"position":[620,300]},{"parameters":{"method":"GET","url":"={{ $json.baseUrl }}/api/config/estabelecimento","options":{"timeout":8000}},"id":"n4000000-4444-4444-4444-444444444444","name":"GET Config Plataforma","type":"n8n-nodes-base.httpRequest","typeVersion":4.2,"position":[840,300]},{"parameters":{"jsCode":"// ============================================================\n// MONTA A MENSAGEM — tudo vem das CONFIGURAÇÕES da plataforma\n// Prioridade: 1) alerta ativo  2) fechado  3) saudação padrão\n// ============================================================\nconst cfg = $input.first().json;\nconst { instance, numero, baseUrl } = $('Roteamento Instância→URL').first().json;\n\nconst nome = cfg.nome_estabelecimento || 'nosso estabelecimento';\nconst link = (cfg.link_exibicao || '').trim();\nconst alerta = (cfg.mensagem_alerta || '').trim();\nconst aberto = !!cfg.aberto;\nconst h = cfg.horario || {};\n\nconst blocoLink = link ? `\\n\\nAcesse nosso cardápio e faça seu pedido pelo link abaixo:\\n🌐 *${link}*` : '';\n\nlet texto;\nif (alerta) {\n  // Adversidade (ex.: chapa queimou) — saudação + alerta + compreensão\n  texto = `Olá! 👋 Aqui é da *${nome}*.\\n\\n⚠️ *Aviso importante:*\\n${alerta}\\n\\nPedimos desculpas pelo transtorno e agradecemos a sua compreensão. 🙏${blocoLink}`;\n} else if (!aberto) {\n  const DIAS = ['Domingo','Segunda','Terça','Quarta','Quinta','Sexta','Sábado'];\n  const dias = (h.dias || []).map(d => DIAS[d]);\n  const diasStr = dias.length === 7 ? 'Todos os dias' : dias.join(', ');\n  texto = `🔒 *Olá! No momento a ${nome} está fechada.*\\n\\nNosso horário de funcionamento:\\n📅 *${diasStr}*\\n🕐 *${h.abertura || ''} às ${h.fechamento || ''}*${blocoLink}\\n\\n_Te esperamos em breve!_ 😊`;\n} else {\n  texto = `Olá! 👋 Seja bem-vindo(a) à *${nome}*! 🍔${blocoLink}\\n\\n_Após finalizar o pedido, você receberá as atualizações aqui pelo WhatsApp!_ 📲`;\n}\n\n// Marca cooldown AGORA (envio vem em seguida)\nconst __sd = $getWorkflowStaticData('global');\nconst cd = __sd.cooldown || {};\ncd[`${instance}::${numero}`] = Date.now();\n__sd.cooldown = cd;\n\nreturn [{ json: { instance, numero, texto, baseUrl } }];"},"id":"n5000000-5555-5555-5555-555555555555","name":"Montar Mensagem","type":"n8n-nodes-base.code","typeVersion":2,"position":[1060,300]},{"parameters":{"method":"POST","url":"={{ $json.baseUrl }}/api/bot/enviar","sendHeaders":true,"headerParameters":{"parameters":[{"name":"Content-Type","value":"application/json"}]},"sendBody":true,"specifyBody":"json","jsonBody":"={\n  \"number\": \"{{ $json.numero }}\",\n  \"text\": {{ JSON.stringify($json.texto) }}\n}","options":{"timeout":10000}},"id":"n6000000-6666-6666-6666-666666666666","name":"Enviar WhatsApp (via plataforma)","type":"n8n-nodes-base.httpRequest","typeVersion":4.2,"position":[1280,300]}],"connections":{"Webhook Evolution":{"main":[[{"node":"Parse + Cooldown","type":"main","index":0}]]},"Parse + Cooldown":{"main":[[{"node":"Roteamento Instância→URL","type":"main","index":0}]]},"Roteamento Instância→URL":{"main":[[{"node":"GET Config Plataforma","type":"main","index":0}]]},"GET Config Plataforma":{"main":[[{"node":"Montar Mensagem","type":"main","index":0}]]},"Montar Mensagem":{"main":[[{"node":"Enviar WhatsApp (via plataforma)","type":"main","index":0}]]}},"settings":{"executionOrder":"v1"},"staticData":{"global":{"cooldown":{}}}}]
